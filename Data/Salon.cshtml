@{
    var db = Database.Open("SalonDB");
    var salonID = Request["id"];
    var sql = @"SELECT Name, Address, City, State, Latitude, Longitude, SalesRep, SixMonthSales FROM Salon WHERE SalonID = @0";
    var data = db.Query(sql, salonID);
    Json.Write(data, Response.Output);
}