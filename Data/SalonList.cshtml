@{
    var db = Database.Open("SalonDB");
    var salonID = Request["id"];
    var sql = @"SELECT SalonID, Name, Address, City, State, Latitude, Longitude FROM Salon WHERE CityStateID = @0 ORDER BY SalonID";
    var data = db.Query(sql, salonID);
    Json.Write(data, Response.Output);
}
