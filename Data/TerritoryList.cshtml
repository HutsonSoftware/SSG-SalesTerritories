@{
    var db = Database.Open("SSG-SalesTerritories");
    var territoryID = Request["TerritoryID"];
    var sql = @"SELECT TerritoryID, EncodedPath, Name FROM Territory ORDER BY TerritoryID";
    var data = db.Query(sql, territoryID);
    Json.Write(data, Response.Output);
}