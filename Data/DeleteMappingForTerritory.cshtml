@{
    var db = Database.Open("SSG-SalesTerritories");
    var territoryID = Request["TerritoryID"];

    var command = "DELETE FROM Mapping WHERE TerritoryID = @0;";
    db.Execute(command, territoryID);
}